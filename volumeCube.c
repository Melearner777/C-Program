#include<stdio.h>
int main(){

    int h,w,d,V;
    printf("Enter the value of h:");
    scanf("%d",&h);
    printf("Enter the value of w:");
    scanf("%d",&w);
    printf("Enter the value of d:");
    scanf("%d",&d);
    V=h*w*d;
    printf("Volume of cubiod is:%d",V);

    return 0;
}